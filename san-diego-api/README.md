# San Diego API 🌞

To use this project, run:

```bash
npm install
npm start
```

See the project running at `localhost:4000`.

Send the following query to test:

```graphql
query CityDetails {
  details {
    avgTemp
    population
    sunshine
  }
}
```
